import 'package:flutter/material.dart';

class Cinco extends StatelessWidget {
  const Cinco({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Pagina Cinco'),
    );
  }
}