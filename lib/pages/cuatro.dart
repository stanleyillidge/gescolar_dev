import 'package:flutter/material.dart';

class Cuatro extends StatelessWidget {
  const Cuatro({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Pagina Cuatro'),
    );
  }
}